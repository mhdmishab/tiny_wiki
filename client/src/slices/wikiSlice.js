import { createSlice } from '@reduxjs/toolkit';
const initialState=null
const wikiSlice = createSlice({
  name: 'wikiSlice',
  initialState ,
  reducers: {
   
  },
  
});

export const {} = wikiSlice.actions;
export default wikiSlice.reducer;